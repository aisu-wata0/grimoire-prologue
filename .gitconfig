[core]
	autocrlf = input
	editor = code --wait
	pager = less -r
[user]
	email = bruno.serbena@gmail.com
	name = Bruno FS
[push]
	default = matching
[diff]
    tool = vscode
[difftool "vscode"]
    cmd = code --wait --diff $LOCAL $REMOTE
[difftool]
	prompt = false
